# review1_1.html 개념 정리

이 문서는 `review1_1.html` 파일에 사용된 주요 웹 기술(HTML, CSS, JavaScript)의 기본 개념을 설명합니다.

## 1. HTML (HyperText Markup Language) - 웹페이지의 뼈대

HTML은 웹페이지의 내용과 구조를 정의합니다.

-   **기본 구조**:
    -   `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`는 모든 HTML 문서의 필수 요소입니다.
    -   `<head>`: 페이지의 설정 정보(제목, 스타일 시트 링크 등)를 포함합니다.
        -   `<title>`: 브라우저 탭에 표시될 페이지 제목을 설정합니다. (예: "연습")
    -   `<body>`: 웹 브라우저 화면에 실제로 표시되는 모든 콘텐츠가 위치합니다.

-   **주요 태그**:
    -   `<h3>`: 제목을 나타내는 태그입니다. `<h1>`부터 `<h6>`까지 있으며, 숫자가 작을수록 더 중요한(일반적으로 더 큰) 제목을 의미합니다.
    -   `<p>`: 문단(Paragraph)을 정의합니다.
    -   `<span>`: 문단 내에서 특정 영역을 묶을 때 사용되는 인라인(inline) 요소입니다. 주로 CSS로 스타일을 적용하거나 JavaScript로 제어할 특정 텍스트 부분을 그룹화하는 데 사용됩니다.
    -   `<div>`: 블록(block) 레벨의 컨테이너로, 여러 요소들을 그룹화하여 CSS로 레이아웃을 제어하거나 JavaScript로 한 번에 관리할 때 사용됩니다.
    -   `<input>`: 사용자로부터 데이터를 입력받을 수 있는 태그입니다.
        -   `type="text"`: 한 줄의 텍스트를 입력받는 필드를 생성합니다.
    -   `<button>`: 클릭 가능한 버튼을 생성합니다.

## 2. CSS (Cascading Style Sheets) - 웹페이지 디자인

CSS는 HTML 요소들의 시각적 표현(디자인과 레이아웃)을 담당합니다. `<style>` 태그 안에 CSS 코드를 작성하여 HTML에 직접 스타일을 적용할 수 있습니다.

-   **선택자 (Selector)**: 어떤 HTML 요소에 스타일을 적용할지 지정하는 규칙입니다.
    -   **태그 선택자**: `p { ... }`
        -   페이지 내의 모든 `<p>` 태그를 선택하여 스타일을 적용합니다.
        -   예: `border: 2px solid blue;` (2픽셀 두께의 파란색 실선 테두리)
    -   **클래스 선택자**: `.group1 { ... }`
        -   `class="group1"` 속성을 가진 모든 요소를 선택합니다. 여러 요소에 동일한 스타일을 적용할 때 유용합니다.
        -   예: `color: magenta;` (글자색을 자홍색으로 변경)
    -   **ID 선택자**: `#my1 { ... }`
        -   `id="my1"` 속성을 가진 **유일한** 요소를 선택합니다. ID는 한 페이지 내에서 고유해야 합니다.
        -   예: `font-size: 2em;` (글자 크기를 기본 크기의 2배로 설정)

> **선택자 요약**:
> *   **Tag**: 해당 종류의 모든 태그에 스타일 적용.
> *   **Class**: 여러 다른 태그에 동일한 스타일 그룹을 적용할 때 사용.
> *   **ID**: 페이지 내에서 단 하나의 고유한 요소에만 스타일을 적용할 때 사용.

## 3. JavaScript - 웹페이지의 동작

JavaScript는 웹페이지를 동적으로 만들고 사용자와의 상호작용을 처리합니다. `<script>` 태그 안에 코드를 작성합니다.

-   **함수 (Function)**: `function show() { ... }`
    -   특정 작업을 수행하는 코드 블록입니다. 여기서는 `show`라는 이름의 함수를 정의했습니다.
    -   `onclick="show()"`: HTML 버튼의 `onclick` 속성은 사용자가 버튼을 클릭했을 때 지정된 JavaScript 함수(`show()`)를 실행하도록 만듭니다.

-   **DOM 조작**: JavaScript는 DOM(Document Object Model)을 통해 HTML 문서의 요소에 접근하고 내용을 변경할 수 있습니다.
    1.  **요소 선택**: `document.querySelector('#input1')`
        -   `document.querySelector()`: CSS 선택자를 사용하여 HTML 문서에서 첫 번째로 일치하는 요소를 찾아 반환합니다.
        -   `'#input1'`은 "ID가 `input1`인 요소를 찾아라"는 의미입니다.
    2.  **값 가져오기**: `input1.value`
        -   `<input>` 요소의 `value` 속성에 접근하여 현재 입력된 값을 가져옵니다.
    3.  **내용 변경**: `output1.innerHTML = `<p>결과 : ${name}</p>`;`
        -   `innerHTML`: 요소 내부의 HTML 콘텐츠를 가져오거나 설정하는 속성입니다.
        -   백틱(\`\`)으로 감싸인 문자열은 **템플릿 리터럴**이라고 하며, `${...}` 구문을 사용하여 문자열 안에 변수 값을 쉽게 삽입할 수 있습니다.
        -   이 코드는 ID가 `output1`인 `<div>` 요소의 내부를 `<p>결과 : ...</p>` HTML로 완전히 교체하여 화면에 결과를 표시합니다.

## 최종 요약

`review1_1.html` 파일은 다음과 같은 흐름으로 동작합니다.

1.  **HTML**로 기본적인 웹페이지 구조(제목, 문단, 입력 상자, 버튼)를 만듭니다.
2.  **CSS**를 사용하여 각 요소에 시각적인 스타일(색상, 크기, 테두리)을 적용합니다.
3.  **JavaScript**를 통해 사용자가 "확인" 버튼을 클릭하는 이벤트를 감지하고, 입력 상자에 있는 텍스트를 읽어와 페이지의 특정 영역에 동적으로 표시합니다.
